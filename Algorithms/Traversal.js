//Breadth-First Search (BFS)
//Explores all vertices in a graph  
//*vertices: elements/objects in a graph connected by edges

//BFS searchs all vertices before moving to the next level
//BFS is typically implemented in queues.

class visitedVertices {
    constructor() {
        this.table = null;
        this.size = 0;
    }

    _hash(key) {
        let index = 0;

        for(let i = 0; i < key.length; i++){
            
        }
    }
}

const bfs = (value) => {
    //empty queue to store vertices it visited
    let queue = [];

    //create hashset or boolean array to keep track of visited vertices

};

//Depth-First Search (DFS)